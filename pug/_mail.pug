form(action="https://app.getresponse.com/add_subscriber.html" accept-charset="utf-8" method="post" class="form-group")
	<!-- Name -->
	input(type="text" name="name" id="name" placeholder="Введите Ваше имя" class="form-control")
	<!-- Email field (required) -->
	input(type="email" name="email" id="email" placeholder="Введите Ваш email*" class="form-control" required)
	<!-- Campaign token -->
	<!-- Get the token at: https://app.getresponse.com/campaign_list.html -->
	input(type="hidden" name="campaign_token" value="nfMbc")
	<!-- Thank you page (optional) -->
	input(type="hidden" name="thankyou_url" value="https://olgaavson.ru/confirm.html")
	<!-- Forward form data to your page (optional) -->
	input(type="hidden" name="forward_data" value="")
	<!-- Subscriber button -->
	input(type="submit" name="submit" value="Записаться" class="btn btn-danger")
	small.besides Вы получите приглашение 
		strong только после 
		| нажатия на ссылку активации, она придёт в письме
	p.politics Нажимая на кнопку, Вы даете согласние на обработку персональных данных
	a(href="/politics.html" target="_blank") Политика конфиденциальности